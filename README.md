# AIPaymentProcessor

Lightweight Python scaffold for an AI-assisted payment processor.

## Quick start

- Create a venv: `python -m venv .venv`
- Install dev deps: `pip install -r requirements.txt`
- Run tests: `pytest`

## Structure

- `aipaymentprocessor/` - package source
- `tests/` - test suite
