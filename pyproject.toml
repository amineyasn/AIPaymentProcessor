[project]
name = "AIPaymentProcessor"
version = "0.1.0"
description = "A lightweight payment processor scaffold using AI"
readme = "README.md"
requires-python = ">=3.8"

[project.urls]
"homepage" = "https://example.com"
